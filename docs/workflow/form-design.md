# 表单设计

表单设计器用于配置流程发起时用户需要填写的表单。通过拖拽字段控件到画布，即可快速构建业务表单。

表单在「工作流 → 表单库」中维护为**可复用表单**，流程设计器通过选择表单进行绑定，多个流程可以复用同一套字段类型与设计器能力。

## 表单类型

流程设计器第二步「表单」支持三种表单来源，可在顶部「表单类型」切换：

| 表单类型 | 说明 | 数据归属 | 适用场景 |
| --- | --- | --- | --- |
| **表单库设计器**（`designer`） | 绑定「表单库」中可视化设计的表单（`formId`），渲染由内置渲染器统一负责 | 流程 `formData` | 标准审批表单，无需编码 |
| **自定义业务表单**（`custom`） | 绑定用户在 `src/pages` 下自行实现的 React 业务页面（`customForm`），创建/查看共用组件按 `mode` 区分 | 流程 `formData` | 业务逻辑复杂、需要复用既有业务页面或自定义交互的场景 |
| **业务系统主导**（`external`） | 由业务模块（自有实体表 + 列表页）自存数据后发起并关联流程，仅配置审批查看组件（`customForm.viewComponent`） | 业务模块自己的表（businessKey 关联） | 已有独立业务实体/列表页，复用工作流做审批编排 |

> `custom` 与 `external` 的核心差异是**数据归属**：`custom` 把表单数据存进流程实例的 `formData`；`external` 的业务数据留在业务模块自己的表，流程只通过 `bizType + bizId` 关联。`external` 的完整接入方式见 [业务模块接入工作流](./business-integration.md)。

## 自定义业务表单

当标准表单控件无法满足业务诉求时，可选择「自定义业务表单」，由业务方自己实现发起填写页与查看页。

### 配置项（设计器 · 表单步骤）

- **创建 / 填写页组件**（必填）：相对 `packages/web/src/pages` 的组件路径，如 `biz/leave/LeaveForm`。解析机制与菜单 `component` 一致（`import.meta.glob`），无需手工注册路由。
- **查看页组件**（可选）：查看样式与填写差异较大时单独配置；留空则复用创建页组件并以只读模式渲染。
- **页签图标**：作为独立页签打开时显示的 lucide 图标（预留能力）。
- **暴露给流程的变量**：声明一组 `{ key, 名称, 类型 }`，供「条件分支」「按表单字段指定审批人」引用。**业务页提交时必须把这些 key 写入 `formData`**，引擎据此求值。

### 业务页面契约

业务组件接收统一 props（类型 `WorkflowBusinessFormProps`，见 `packages/web/src/components/workflow/BusinessFormHost.tsx`）：

| prop | 说明 |
| --- | --- |
| `mode` | `create`=发起填写、`view`=只读查看、`approve`=审批办理 |
| `container` | `sheet`=嵌入抽屉（当前）、`tab`=整页多页签（预留） |
| `definitionId` / `instanceId` | 所属流程定义 / 实例 id |
| `value` | 创建时为初始值；查看/审批时为已保存的业务 `formData` |
| `readOnly` | 是否只读（`view` 模式恒为 `true`） |
| `variables` | 设计器声明的流程变量 |
| `getFormApi` | 注册命令式 API `{ validate, getValues }`，供宿主在提交时取值校验（`create`/`approve` 必需） |

发起工作台与「我的申请」详情均通过 `BusinessFormHost` 承载业务页面：发起时取 `getFormApi().validate()` 的返回值作为实例 `formData` 提交；查看时把实例 `formData` 作为 `value` 传入并只读渲染。可参考示例页 `packages/web/src/pages/biz/demo/DemoBusinessForm.tsx`。

> 自定义业务表单的数据对引擎是**不透明**的（实例只冻结组件配置与变量声明，不冻结设计器字段），仅声明的变量参与条件分支/审批人计算，因此业务页需保证提交数据包含这些变量 key。

## 设计器布局

表单设计器采用三栏布局：

- **左侧控件面板**：可用字段类型列表，拖拽到中间画布即可添加
- **中间画布**：表单预览区，支持拖拽排序、嵌套布局
- **右侧属性面板**：选中字段后显示该字段的配置选项

## 支持的字段类型

表单字段共 **40 种**，左侧控件面板按以下分组展示。

### 基础控件

| 字段类型 | 名称 | 典型用途 |
| --- | --- | --- |
| `text` | 单行文本 | 标题、名称、简述 |
| `textarea` | 多行文本 | 详细说明、备注 |
| `number` | 数字 | 数量、不带货币的数值 |
| `amount` | 金额 | 带币种的金额，支持精度设置 |
| `date` | 日期 | 单个日期选择 |
| `dateRange` | 日期区间 | 起止日期选择 |
| `time` | 时间 | 时间选择 |

### 选择控件

| 字段类型 | 名称 | 典型用途 |
| --- | --- | --- |
| `select` | 下拉单选 | 单选场景 |
| `multiSelect` | 下拉多选 | 多选场景 |
| `autoComplete` | 自动完成 | 带建议项的输入 |
| `radio` | 单选框组 | 选项较少的单选 |
| `checkbox` | 复选框组 | 多选场景 |
| `switch` | 开关 | 是/否布尔值 |
| `slider` | 滑块 | 数值区间选择 |
| `tags` | 标签 | 多标签录入 |
| `colorPicker` | 颜色 | 颜色选择器 |

### 格式化控件

| 字段类型 | 名称 | 典型用途 |
| --- | --- | --- |
| `phone` | 手机号 | 手机号输入，带格式校验 |
| `email` | 邮箱 | 邮箱输入，带格式校验 |
| `idCard` | 身份证 | 身份证号，带格式校验 |
| `url` | 网址 | URL 链接 |
| `password` | 密码 | 掩码密码输入 |
| `pinCode` | 验证码 | 定长验证码 / PIN 码 |
| `rate` | 评分 | 星级评分 |
| `formula` | 公式 | 从其他字段自动计算（只读） |

### 系统组件

| 字段类型 | 名称 | 典型用途 |
| --- | --- | --- |
| `userSelect` | 人员选择 | 选择系统内用户，支持多选 |
| `deptSelect` | 部门选择 | 选择组织部门，支持多选 |
| `dictSelect` | 字典选择 | 选择数据字典项（绑定 `dictCode`） |

### 高级控件

| 字段类型 | 名称 | 典型用途 |
| --- | --- | --- |
| `attachment` | 附件 | 上传文件，可设最大数量 |
| `image` | 图片 | 上传图片，可设最大数量 |
| `region` | 省市区 | 省/市/区联动选择 |
| `signature` | 手写签名 | 手写签名板 |
| `richtext` | 富文本 | 富文本编辑器 |
| `detail` | 明细 | 明细/子表格，内含子字段，可汇总 |

### 布局控件 / 布局

| 字段类型 | 名称 | 说明 |
| --- | --- | --- |
| `description` | 说明文字 | 纯展示文本，用于提示说明 |
| `serialNumber` | 流水号 | 自动生成流水号，支持前缀 |
| `row` | 分栏 | 网格/分栏布局，每栏可放置字段 |
| `divider` | 分割线 | 视觉分隔 |
| `group` | 分组 | 带标题的分组区块 |
| `tabs` | 标签页 | 多面板标签页切换，每个标签可放置字段 |
| `steps` | 分步 | 向导式分步填写，逐步上一步 / 下一步 |

## 字段配置

选中画布上的字段后，右侧属性面板可配置：

**基础属性**：
- **字段名称**：显示在表单上的标签
- **字段标识**：表单数据的 key，建议英文命名，如 `leaveType`、`startDate`
- **是否必填**：勾选后该字段必须填写才能提交
- **占位提示**：输入框中的提示文字
- **帮助说明**：字段下方的补充提示
- **默认值**：字段初始值
- **栅格占宽 / 标签位置**：控制字段在表单中的布局宽度与标签对齐方式

**扩展属性**（按字段类型不同）：
- **选项值 / 选项联动**（select/multiSelect/radio/checkbox/autoComplete）：配置选项列表，支持按父字段值级联生成
- **选项增强**（select/multiSelect/radio/checkbox）：选项可单独设置「值 / 显示名分离」、标签颜色、禁用某项
- **允许填写「其他」**（select/radio）：在固定选项外允许用户填写自定义值
- **长度 / 正则**（text/textarea/格式化控件）：最小/最大长度、正则校验及提示
- **精度 / 单位 / 范围**（number/amount/slider）：小数位数、单位、最小/最大值、步长
- **货币**（amount）：人民币 / 美元 / 欧元 / 英镑 / 日元
- **日期格式 / 时间格式**（date/dateRange/time）：如 `年-月-日`、`时:分:秒`
- **可选范围**（date/dateRange）：不可选过去 / 不可选未来 / 自定义最早最晚日期
- **最大数量 / 文件类型 / 单文件大小**（attachment/image）：上传数量、允许的扩展名或 MIME、单文件 MB 上限
- **最大数量**（tags/pinCode）：标签数 / 验证码位数
- **评分上限**（rate）、**透明度**（colorPicker）、**省市区层级**（region）
- **多选**（userSelect/deptSelect/dictSelect）、**字典编码**（dictSelect）
- **公式表达式**（formula）：使用 `{字段key}` 引用其他字段，内置数学 / 逻辑 / 文本 / 日期函数库（见下）
- **前缀**（serialNumber）：流水号前缀
- **面板标题**（tabs/steps）：增删标签页 / 步骤，编辑每个面板标题
- **可见性规则**：基于其他字段值（且/或组合）动态显示/隐藏该字段

**校验规则**（「校验规则」页签）：
- **禁止重复**（text/number/amount/格式化控件）：在明细表内该列值不可重复
- **字段比较校验**（number/amount/date）：当前值与目标字段比较（>、≥、<、≤、=、≠），不满足时阻断提交并提示

## 公式函数库

公式字段（`formula`）支持以 `{字段key}` 引用其他字段、`{明细key.列key}` 引用明细列（聚合），并提供四类函数（配置面板可点击插入）：

| 分类 | 函数 |
| --- | --- |
| 数学 | `SUM` `AVG` `MAX` `MIN` `COUNT` `ROUND` `ABS` `CEIL` `FLOOR` `MOD` `POWER` `SQRT` |
| 逻辑 | `IF` `AND` `OR` `NOT` |
| 文本 | `CONCAT` `LEN` `LEFT` `RIGHT` `MID` `UPPER` `LOWER` `TRIM` `REPLACE` `TEXT` |
| 日期 | `TODAY` `NOW` `YEAR` `MONTH` `DAY` `DATE` `DATEDIF` |

示例：`IF({days}>3, {amount}*0.9, {amount})`、`SUM({items.amount})`、`CONCAT({firstName}, {lastName})`、`DATEDIF({start}, {end}, "d")`。公式同时支持 `+ - * / %`、比较与三元运算。

## 布局能力

### 分栏（Row）

将字段拖拽到分栏组件中，可实现横向并排布局。分栏支持自定义每栏的宽度比例（如 6:6、4:8、3:9 等）。

### 分组（Group）

将相关字段放入分组中，可设置分组标题。分组可展开/收起，提升表单可读性。

### 分割线（Divider）

在不同区块之间添加视觉分隔线。

### 标签页（Tabs）

将字段分配到多个标签页中，运行时点击标签切换显示，适合字段较多、可按维度归类的表单。所有标签页字段始终参与校验与提交。

### 分步（Steps）

向导式分步填写：顶部显示步骤进度条，逐步「上一步 / 下一步」浏览，适合较长的录入流程。所有步骤字段始终参与校验与提交。

## 表单字段在流程中的使用

### 条件分支中使用

在流程设计的条件分支中，可以引用表单字段的值作为分支条件。例如：

```
如果 请假类型 == "病假" 且 天数 > 3
则走 "病假审批" 分支
否则走 "普通审批" 分支
```

### 节点表单权限

在审批/办理节点的配置中，可以设置每个表单字段的权限：

| 权限 | 说明 |
| --- | --- |
| 可读 | 审批人可以看到该字段的值，但不能修改 |
| 可编辑 | 审批人可以修改该字段的值 |
| 隐藏 | 审批人看不到该字段 |

字段是否必填由表单字段自身的「是否必填」属性控制；节点权限只控制该节点处理人看到字段时是可读、可编辑还是隐藏。

例如：在"请假申请"流程中，发起人可以填写"请假原因"，而审批人只能看到但不能修改。

### 审批人指定中使用

在审批人节点配置中，可以使用表单字段来动态指定审批人：

- **表单内联系人**：选择表单中的人员字段作为审批人
- **表单内部门**：选择表单中的部门字段，自动取部门负责人作为审批人

## 设计规范建议

1. **字段标识使用英文**：便于后续在条件分支、审批人策略中引用
2. **合理分组**：将相关字段放入同一分组，提升表单可读性
3. **必填项标注**：关键信息设置为必填，避免提交后数据不完整
4. **预留说明字段**：在复杂表单中添加说明文字，引导用户填写
